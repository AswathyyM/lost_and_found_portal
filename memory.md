## Empty State Handling
- Implemented Django template `{% empty %}` fallback inside the main items loop.
- Displayed the standard "No lost/found items available" message with a dashed card layout for clear user feedback when no listings exist.